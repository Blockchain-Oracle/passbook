{
  "name": "@strk20/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --configLoader native",
    "//build": "Deliberately absent. `pnpm run build --filter @strk20/web` (and npm's `-w` equivalent) is the door most deploy platforms take by default, and a bare `vite build` bypasses EVERY gate in this repository: the warning contract, the eager-bundle budget, the SDK-marker scan and the browser evaluation that is the only thing proving the artifact is not dead on arrival. Use `npm run build:web` from the repo root. `build:raw` below is the unguarded escape hatch — named so nothing reaches it by accident.",
    "build:raw": "vite build --configLoader native",
    "preview": "vite preview"
  },
  "dependencies": {
    "@base-ui/react": "1.7.0",
    "@fontsource-variable/ibm-plex-sans": "5.3.0",
    "@strk20/protocol": "workspace:*",
    "@tanstack/react-router": "1.170.32",
    "class-variance-authority": "0.7.1",
    "clsx": "2.1.1",
    "qrcode.react": "4.2.0",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "tailwind-merge": "3.6.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "4.3.3",
    "@tanstack/router-core": "1.171.27",
    "@tanstack/router-plugin": "1.168.35",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.5",
    "@vitejs/plugin-react": "6.1.0",
    "tailwindcss": "4.3.3"
  }
}
